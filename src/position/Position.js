import React, { Component } from "react";
import styles from "../position/position.css";

class Position extends Component {
  constructor(props)
  {
    super(props)
  }
  render() {
    return (
      <div>
         <div id="map">asdfas</div>
      </div>
    );
  }
}

export default Position;
